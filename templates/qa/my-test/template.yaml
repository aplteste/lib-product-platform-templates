apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: my-test
  title: Test
  description: Test vamos la
  annotations:
    selfservice.devportal.yape.tech/visible: "true"
    selfservice.devportal.yape.tech/icon: icWorld
    selfservice.devportal.yape.tech/color: violet
spec:
  owner: Product Platform
  type: service
  parameters:
    - title: New page
      required:
        - name
      properties:
        name:
          title: name
          type: string
